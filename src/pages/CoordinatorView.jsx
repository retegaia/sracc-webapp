// Route /coordinator (Tab.3) — AggregatedView/PervasivityView/SignalView sono S4 (Tab.6).
// Placeholder minimo per non lasciare la route vuota nel frattempo.
export default function CoordinatorView() {
  return <p>Vista coordinatore — in arrivo (S4).</p>
}
